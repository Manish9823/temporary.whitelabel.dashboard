import {
    Grid,
    Typography
} from "@mui/material";
import React from "react";

export default function AllUnitDashboard() {
    return (
        <React.Fragment>
            <Grid>
                <Grid>Organization Dashboard</Grid>
            </Grid >
        </React.Fragment>
    );
}